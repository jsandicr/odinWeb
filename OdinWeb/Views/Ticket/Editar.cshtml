@model OdinWeb.Models.Obj.Ticket
@{
    ViewData["Title"] = "Editar Ticket";
}
<div class="col-md-12">
    <div class="tile">
        <h3 class="tile-title">Editar Ticket</h3>
        <div class="tile-body">
            <form asp-action="Actualizar" asp-controller="Ticket" method="post">
                <input type="hidden" asp-for="id">
                <div class="form-group">
                    <label asp-for="title" class="control-label"></label>
                    <input asp-for="title" class="form-control" type="text">
                </div>
                <div class="form-group">
                    <label asp-for="description" class="control-label"></label>
                    <input asp-for="description" class="form-control" rows="4">
                    <span asp-validation-for="description"></span>
                </div>
                <div class="form-group">
                    <label asp-for="creationDate" class="control-label"></label>
                    <input asp-for="creationDate" class="form-control" rows="4">
                    <span asp-validation-for="creationDate"></span>
                </div>
                <div class="form-group">
                    <label asp-for="updateDate" class="control-label"></label>
                    <input asp-for="updateDate" class="form-control" rows="4">
                    <span asp-validation-for="updateDate"></span>
                </div>
                <div class="form-group">
                    <label asp-for="closeDate" class="control-label"></label>
                    <input asp-for="closeDate" class="form-control" rows="4">
                    <span asp-validation-for="closeDate"></span>
                </div>
                <div class="form-group">
                    <label asp-for="estimatedDate" class="control-label"></label>
                    <input asp-for="estimatedDate" class="form-control" rows="4">
                    <span asp-validation-for="estimatedDate"></span>
                </div>
                <div class="form-group">
                    <label asp-for="active" class="control-label"></label>
                    @foreach (var opcion in (List<SelectListItem>)ViewData["Estados"])
                    {
                        <div class="form-check">
                            <label class="form-check-label">
                                <input asp-for="active" class="form-check-input" type="radio" name="active" value="@opcion.Value"> @opcion.Text
                            </label>
                        </div>
                    }
                </div>
                <div>
                    <label>Seleccione el cliente</label>
                    @Html.DropDownList("idClient", (IEnumerable<SelectListItem>)ViewData["Clients"], new { @class = "form-control form-select mb-3" })
                </div>
                <div>
                    <label>Seleccione el supervisor</label>
                    @Html.DropDownList("idSupervisor", (IEnumerable<SelectListItem>)ViewData["Supervisors"], new { @class = "form-control form-select mb-3" })
                </div>
                <div>
                    <label>Seleccione el servicio</label>
                    @Html.DropDownList("idService", (IEnumerable<SelectListItem>)ViewData["Services"], new { @class = "form-control form-select mb-3" })
                </div>
                <div>
                    <label>Seleccione el status</label>
                    @Html.DropDownList("idStatus", (IEnumerable<SelectListItem>)ViewData["Status"], new { @class = "form-control form-select mb-3" })
                </div>
                <div class="tile-footer">
                    <button class="btn btn-primary" type="submit"><i class="fa fa-fw fa-lg fa-check-circle"></i>Modificar</button>&nbsp;&nbsp;&nbsp;<a class="btn btn-secondary" asp-action="Home" asp-controller="Ticket"><i class="fa fa-fw fa-lg fa-times-circle"></i>Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>

